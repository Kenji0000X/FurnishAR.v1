{
  "name": "furnishar",
  "version": "1.1.0",
  "private": true,
  "description": "FurnishAR - browser-native furniture planning for Mamburao retailers",
  "scripts": {
    "local": "node local.js",
    "dev": "next dev",
    "next:build": "next build",
    "next:start": "next start",
    "test": "node --test tests/*.test.js",

    "//": "The vanilla site is still what deploys while the Next.js port lands route by route. `build` stays pointed at it until /plan and /portal are ported; see docs/FRAMEWORK.md.",
    "dev:legacy": "node --watch local.js",
    "build": "node build.js",
    "test:db": "node --test tests/db.test.js",
    "check:supabase": "node scripts/check-supabase.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "next": "^16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "three": "^0.170.0"
  }
}
